server:
  port: 8089

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/1248664896/spring
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8080/eureka/


#      spring:
#        cloud:
#          config:
#            server:
#              git:
#                uri指向的是配置文件所在的git项目uri: